import { DefaultTheme } from "styled-components";

export const theme: DefaultTheme = { // DefaultTheme에 항목들이 더 있는 상황에서 여기에 설정을 하지 않았으면 오류라고 알려줌
    bgColor: "#2f3640",
    textColor: "#f5f6fa",
    accentColor: "#fbc531",
}
